plugins {
	id 'java'
	id 'org.springframework.boot' version '{version}'
}

bootBuildImage {
	builder = "ghcr.io/spring-projects/spring-boot-cnb-builder:0.0.3"
	buildCache {
		volume {
			name = "build-cache-volume1"
		}
		volume {
			name = "build-cache-volum2"
		}
	}
}
